const container = document.getElementById('root'); // get element with id root
const  root = ReactDOM.createRoot(container); // create root with container

root.render(<h1>Belajar React bareng WPU </h1>); // render h1 element to root

// Shortcut Ctrl+Shift+P -> "ESLint: Fix all auto-fixable problems" akan memperbaiki masalah otomatis.


function UnformattedComponent() {return(
    <div> Unformatted code example with some issues.let's fix this code later.</div>
    )}export default UnformattedComponent;
    
    